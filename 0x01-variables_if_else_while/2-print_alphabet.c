#include <stdio.h>

/**
 * main - 2. I sometimes suffer from insomnia.
 *
 * Return: Always (Success)
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);

	putchar('\n');
	return (0);
}
