#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _abs - function that computes the absolute value of an integer
 *
 * Return: int
 */
int _abs(int n)
{
	int a;

	a = abs(n);
	return (a);
}