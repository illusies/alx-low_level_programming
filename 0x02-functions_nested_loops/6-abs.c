#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _abs - function that computes the absolute value of an integer
 *
 * Return: int
 */
int _abs(int i)
{
	int a;

	a = abs(i);
	return (a);
}